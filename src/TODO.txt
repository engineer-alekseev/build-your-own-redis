Network layer:
Цель проекта: повторить и углубить понимание работы стека TCP/IP на реальной задаче

Вводные данные: 
- книга https://build-your-own.org/#section-redis, что не так в книге:
-- C-style
-- Весь код 
-- poll, хотя сейчас оптимальным на linux является epoll
-- весь код в структурной парадигме
- В стандартной библиотеке С++17 нет поддержки для программирования сетевых соединений

Что планируется сделать
1. Воспроизвести примеры из глав 1-6 книги как есть, разобраться как это работает
2. Попробовать самостоятельно переделать код на epoll
3. Переписать весь код на ООП подход
-- сначала в виде конкретных классов
-- далее возможно как-то обобщить
-- сравнить получившуюся реализацию с тем, что уже есть (поискать аналоги, например реализацию из Qt, Boost)

Что есть:
- Сейчас реализовано неблокирующее API - весь код из ч.1 гл. 1-5, как есть(nonblocking_io_client.cpp/nonblocking_io_server.cpp)
- Что с ним не так:
-- C-style
-- Дублирование кода

Следующие задачи:
1. Попробовать реализовать более правильную работу с буфферизацией, как это описано в главе 6.
2. Переделать на epoll
3. Переписать в ООП стиле (пока простые конкретные классы сервера/клиента)
4. Получить обратную связь, обсудить, что можно улучшить (выделить абстракции, шаблонизировать, оформить в виде библиотеки)

Вопросы и заметки для ментора:
!1. Кажется ревьюить сейчас преждевременно, так как это по большей части С код из книги 
?2. Порядок разработки:
- сначала переписываем на С++ стиль, а потом вносим улучшения (epoll, буфферизация и т.д.) или наоборот?
?3. Вероятно имеет смысл сразу писать тесты под этот код, уточнить что использовать и как?
!4. Было бы интересно пообщаться по теории TCP/UDP в формате мок-собеседования, чтобы уточнить, до какого уровня вообще все это есть смысл копать, что из 
этого вообще должен знать прикладной разработчик



