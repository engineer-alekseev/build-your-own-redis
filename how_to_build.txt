# 1. Создаём отдельный каталог для сборочных файлов
# этот шаг выполняем, только если меняется CMakeLists.txt
cmake -B build -DCMAKE_BUILD_TYPE=Debug

# этим пользумся, когда пересобираем после правок исходного кода
# 2. Собираем (по умолчанию будет использован Makefiles; добавьте -G Ninja, если хотите Ninja)
cmake --build build